import TrainingMode from '@/components/training/TrainingMode';

export const metadata = { title: 'Training Mode — Capelli CS Coach' };

export default function TrainingPage() {
  return <TrainingMode />;
}
