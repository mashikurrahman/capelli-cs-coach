import UserManagement from '@/components/admin/UserManagement';

export const metadata = { title: 'User Management — Capelli CS Coach' };

export default function UsersPage() {
  return <UserManagement />;
}
