import WorkflowLibrary from '@/components/workflows/WorkflowLibrary';

export const metadata = { title: 'Workflow Library — Capelli CS Coach' };

export default function WorkflowsPage() {
  return <WorkflowLibrary />;
}
